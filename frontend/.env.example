# 前端环境变量配置
# 复制此文件为 .env 或 .env.local

# 后端服务地址 (用于 vite 代理)
VITE_BACKEND_HOST=127.0.0.1
VITE_BACKEND_PORT=18000

# 管理后台路由前缀 - 需要与后端 ADMIN_ROUTE_PREFIX 保持一致
# 修改后需重新构建前端: pnpm build
VITE_ADMIN_ROUTE_PREFIX=/console-x7k9m
